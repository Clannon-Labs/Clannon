[package]
name = "backend-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
# tokio.workspace = true
# serde.workspace = true
# serde_json.workspace = true
thiserror.workspace = true

[workspace]
resolver = "3"

members = ["crates/foundation",
    # crates/api"
    # "crates/auth",
    # "crates/email",
]

[workspace.package]
edition = "2024"

[workspace.dependencies]
# tokio = { version = "1", features = ["full"] }
# serde = { version = "1", features = ["derive"] }
# serde_json = "1"
thiserror = "2.0.19"

# Add your internal workspace crates here whose definitions can be inherited by
# any crate in the workspace.
# Example:
foundation = { path = "crates/foundation" }
# api = { path = "crates/api" }

[dev-dependencies]
foundation = {workspace = true}
