# Phase 5 — Capability Work (close V1 gaps)   STATUS: not started

**Goal:** advance benchmarks in priority order; every claimed PASS needs a
committed runnable proof (else it stays PARTIAL).

- [ ] **CB5** (do first, near-pass): close the detect residual + surface the
      verifier's classify/explain as structured output; widen the c5 battery.
- [ ] **CB1** (+EB1): additive fact|assumption + valid_at/superseded_by typing on
      MemoryWriteProposal/MemoryItem (ROBUST_MEMORY §7.4 additive contract) +
      surfaced retrieval "why"; extend c1 + e1 harnesses to PASS.
- [ ] **CB4** (+CB6/EB2): durable decision-log audit mirror — persist the in-run
      decision log as structured records. **Propose-first** (new persistence
      surface). Extend c4 harness.
- [ ] **CB2/CB3/EB3**: Kuzu knowledge-web. **Propose-first — write a build spec in
      docs; do NOT half-build.** Only attempt a thin static-import-graph slice for
      a minimal CB2 if the owner approves the spec.

**Acceptance:** each advanced benchmark has a committed proving test; the graph
build spec exists (not built) with a propose note.
